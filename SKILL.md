---
name: generate-daily-report
description: >-
  生成当天中文工作日报，并保存到当前项目根目录的
  docs/daily-report/yyyy-MM-dd 日报.md。用于用户说“生成日报”“写日报”“整理今天工作”，
  或要求从当前 Git 项目总结今日工作时。先同步所有分支，按当前 Git 用户名称/邮箱筛选当天相关记录，
  再基于这些记录及其 diff 判断功能完整性；同时提取障碍、风险、痛点、堵点、沉淀候选，
  按 grilling 轮次追问用户决定保留内容，最后以 Humanizer-zh 风格定稿。
---

# 生成日报

按“同步所有分支 → 解析当前 Git 身份 → 筛选当天相关记录 → 分析记录 diff → 判断功能完整性 → 翻译成面向审计者的工作结论 → 候选 grilling → 用户确认 → 写入日报”的顺序执行。每一步结束前满足完成条件；证据不足时保留“不确定”，不补写事实。

## 1. 同步分支并确定 Git 身份

1. 使用当前运行环境的本地日期作为 `REPORT_DATE`，格式为 `yyyy-MM-dd`。用户明确指定日期时，使用用户指定的日期。
2. 运行 `git rev-parse --show-toplevel` 找到当前项目根目录，并记录当前分支、上游分支和采集时间窗口。没有 Git 仓库时，说明无法采集代码证据，并询问是否只根据口述生成。
3. 从当前仓库配置读取 `git config --get user.name` 和 `git config --get user.email`。将它们作为当前 Git 身份；名字缺失时使用邮箱，名字和邮箱都缺失时先向用户询问，不猜测身份。
4. 先运行 `git fetch --all --prune` 同步所有远程分支，再用 `git for-each-ref` 或 `git branch -a`记录 `refs/heads` 和 `refs/remotes`。fetch 失败时保留错误状态，继续读取已有 refs，但在日报中标明数据可能不完整。
5. 从本 Skill 目录解析 `scripts/collect_git_evidence.sh` 的绝对路径，并运行 `bash <skill-dir>/scripts/collect_git_evidence.sh --date "$REPORT_DATE" --repo "$REPO_ROOT"`。脚本默认执行 fetch；仅在用户明确要求离线分析时才使用 `--no-fetch`。脚本不修改工作区文件、不执行 commit 或 push。
6. 将 `docs/daily-report/` 作为日报输出目录。采集工作区 diff 时排除这个目录，避免把本次日报自身算成工作候选。

完成条件：明确报告日期、仓库根目录、fetch 状态、Git 用户名/邮箱、分支列表、当前分支、上游分支和时间窗口。

## 2. 筛选当天当前用户相关的 Git 记录

### 2.1 全 refs 精确筛选

1. 在 fetch 之后使用 `git log --all --since="$REPORT_DATE 00:00:00" --until="$REPORT_DATE 23:59:59"` 搜索所有本地分支和远程跟踪分支；不要只看当前分支或最近一次 commit。
2. 将 commit 的 author name、author email、committer name、committer email 与当前 `user.name`/`user.email` 做精确匹配。author 或 committer 任一身份匹配即可纳入；记录匹配的是作者还是提交者。
3. 每个 SHA 只处理一次。为每条匹配记录保留 SHA、作者、提交者、时间、主题、涉及文件、所在 refs 和上游可见状态；用 `git branch -a --contains <sha>`核对它出现在哪些分支。
4. 对每条匹配记录运行 `git show --stat --name-status <sha>`，必要时查看 `git show --format=fuller --no-ext-diff <sha>`，从实际 diff 总结工作，不从 commit 标题单独猜结果。
5. 如果 fetch 成功但没有匹配记录，明确写“当天没有发现与当前 Git 身份匹配的记录”。如果只有其他用户记录，不得把它们算作本人完成工作；可以作为上下文，但须单独标注。

### 2.2 处理当前工作区 diff

1. 用 `git diff HEAD`、`git diff`、`git diff --cached` 和 `git status --short --untracked-files=all` 识别当前工作区改动，并排除 `docs/daily-report/`。
2. 工作区 diff 没有对应当天用户记录时，仍可作为“未提交工作区候选”，但先询问用户是否属于本人今日工作；不得因为它出现在当前目录就自动归给当前 Git 用户。
3. 对 `.env`、密钥、证书、凭据和大文件只记录类型与风险，不读取或写入敏感内容。

完成条件：得到一份去重后的“当天当前 Git 用户记录”清单，以及一份单独标注归属待确认的工作区 diff 清单。

## 3. 基于记录 diff 判断功能完整性

只要匹配记录或确认归属的工作区候选存在 Git diff，就先列入“今日完成候选”。commit 是否已上游可见只作为来源状态，不决定功能是否完成。

对每个候选执行以下判断：

1. 从代码、调用方、测试、配置和文档推断这组改动要解决的功能。
2. 检查功能是否形成闭环：入口是否接通，主流程是否可执行，关键成功/失败分支是否处理，必要配置/迁移/依赖是否齐全，测试或构建证据是否与结论一致。
3. 存在 TODO/占位实现、半截流程、未接通调用方或注册处、失败测试/构建/静态检查、缺失错误路径/权限/边界处理、未满足依赖，或用户确认目标尚未达成时，归入“今日未完成工作”。
4. 功能闭环且没有相反证据时，归入“今日完成工作”。“工作区未提交”“仅本地提交”“上游引用已可见”等状态只保留在内部证据中，不进入默认日报正文。无法判断时标为“完整性待确认”，交给 grilling，不自行判成完成。
5. 同一功能的多个 commit、不同分支上的同一 SHA、暂存和未暂存 diff 合并成一个工作点，保留关键短哈希、分支或文件名作为证据。合并提交、格式化提交、物化提交或日报文件改动不得自动解释成业务完成。

完成条件：每个当前用户相关记录及每个归属确认的工作区 diff 都有“完成、未完成或待确认”的功能结论，且结论能指向实际 diff、测试结果或用户回答。

## 4. 将技术证据翻译成可审计的工作结论

日报的默认读者没有参与项目，也不熟悉当前 Skill、代码结构或研发流程。Git、测试、构建和工作区状态只用于内部判断；写入日报前，必须把证据翻译成读者能直接理解的“做成了什么、解决什么问题、现在呈现什么结果”。

1. 将同一目标下的代码、脚本、配置、资源和文档改动合并成一个工作点，不按文件清单逐条罗列。
2. 用结果和能力描述工作，不用实现动作描述工作。例如，把“新增采集脚本和配置文件”改写为“建立了按日期和当前开发者身份汇总代码变更的能力”。
3. 只有能说明用途或可观察结果的改动才进入“今日完成工作”。初始化、补文件、改配置、加图标、补说明等动作本身不算独立完成项；如果没有可见结果，合并说明或省略。
4. 每个工作点回答：完成了什么；为项目或团队解决了什么具体问题；当前呈现什么结果。没有证据的影响、用户反馈和业务价值不要补写。
5. 测试通过、构建通过、静态检查通过、命令执行成功等内容用于确认结论，不作为完成项的正文。只有失败结果直接造成未完成、风险或堵点时，才用业务可理解的方式说明影响。
6. “工作区未提交”“仅本地提交”“尚未推送”“已推送”“已同步到上游”等 Git 状态不得进入默认正文。只有用户明确要求版本追溯，或该状态直接导致他人无法使用、评审或发布时，才说明其实际影响。
7. 技术术语首次出现时用一句话解释；非必要不写命令、内部路径、文件名、短哈希、作者字段、分支字段或上游字段。
8. 生成正文前检查并删除 Skill 自身的名称、目录名、内部编号及其实现文件名，包括 generate-daily-report、skill-<id>、SKILL.md、scripts/collect_git_evidence.sh、agents/openai.yaml、assets/icon.svg、references/humanizer-zh.md、README.md、LICENSE 等。项目业务文件只有在它能帮助第二个读者核对结果时才可保留，并且要说明其用途。
9. 不把“完成了 Skill 初始化”“补充了包内资源”“新增了某个内部文件”写成日报成果。应改成对应的用户可理解能力；若只能证明工程准备工作，明确写成“建立基础能力”，不要夸大为业务功能已完成。

错误写法：“详情页已完成优化，前端开发环境构建通过；当前改动仍在工作区，尚未提交。”
推荐写法：“优化知识资产详情展示，明确区分发布时间和更新时间，并支持从总览列表查看单条发布结果详情。”

错误写法：“完成日报 Skill 初始化，新增多个内部文件并补充配置。”
推荐写法：“建立了按日期和当前开发者身份汇总代码变更的日报基础能力。”

推荐写法还应根据实际 diff 补充闭环状态；如果存在失败分支或功能缺口，就把对使用结果的影响写入“今日未完成工作”或“风险”，不要用研发验证过程充当工作成果。

完成条件：每个日报工作点都能由不了解项目的读者读懂，并且仍能回溯到内部记录、实际 diff、验证结果或用户回答；正文不暴露 Skill 包内部实现清单。

## 5. 从记录 diff 提取第三、第四项候选

在 grilling 前阅读当前用户相关记录的 diff 及其上下文，生成带证据和置信度的候选清单。候选是待用户选择的材料，不是最终结论。

- 障碍：TODO、失败测试、缺失依赖、未接通接口、未解决冲突、环境/权限前置条件或明确卡住的代码路径。
- 风险：认证、权限、数据结构、迁移、公共接口等敏感改动；缺少测试的分支；硬编码配置；大范围改动；破坏兼容性的行为变化；缺少回滚或失败处理。
- 痛点：diff 中可直接观察到的重复逻辑、临时绕过、手工步骤、脆弱分支、复杂调用链或反复修改的区域。没有代码迹象时只写“待用户确认”。
- 堵点：外部服务、评审、环境、账号、依赖、接口或决策尚未满足，导致功能无法闭环的具体前置条件。
- 沉淀：新增测试、工具函数、排查路径、根因判断、技术取舍、配置规则、可复用命令、文档或用户确认的决策。

为每个候选保留“候选内容 + diff 证据 + 可能影响/复用价值 + 置信度”。不要把同一事实同时硬塞进障碍、风险和痛点；按用户最终选择放入最合适的类别。

完成条件：第三、第四项的候选都来自当前用户相关记录的 diff、确认归属的工作区 diff，或明确标注为待用户补充；没有未经确认的推断。

## 6. 用 grilling 让用户决定保留内容

采用设计树和 frontier 分轮提问。仓库能查到的事实由自己查；只向用户询问目标、体验、判断、影响和取舍。每轮问当前所有前置条件已满足的问题，等待回答后再重算下一轮。
向用户展示候选时使用第 4 节生成的读者友好工作点，不直接粘贴 git show 输出、内部文件清单或 Skill 包路径。只有用户明确要求精确追溯时，才补充短哈希、分支或具体文件。

第一轮根据记录数量合并问题，至少覆盖以下未决项：

```text
❓ Q1 - 记录与功能完整性：<逐项列出匹配记录/工作区候选，判断“属于本人 / 不属于 / 待确认”和“完成 / 未完成 / 待确认”>

➡️ 建议回答：<根据当前 Git 身份、分支和 diff 给出的候选判断；用户可修正>

❓ Q2 - 日报保留项：<列出障碍、风险、痛点、堵点、沉淀候选>

➡️ 建议回答：<逐项标记“保留 / 删除 / 修改”，并给出推荐选择>
```

继续追问时：

1. 用户标记“修改”或“待确认”的候选，沿着“预期结果 → 当前状态 → 卡点/影响 → 用户希望的表述”继续问。
2. 对每个未完成功能追问停留位置、缺口、影响和下一步所需条件；对每个沉淀候选追问是否值得复用以及用户希望留下的具体说法。
3. 用户标记“删除”的候选不写入日报；用户标记“保留”的候选保留事实来源，并按用户修正后的表述定稿。
4. 用户说“跳过”时写“用户未补充”，不把推荐答案当作用户确认。所有 frontier 都有答案后，复述第三、第四项最终摘要，请用户确认或修正。

完成条件：用户已经决定每个候选的归属、完整性和保留/删除/修改方式；未确认内容显式标为“待补充”，没有静默假设。

## 7. 编写四项日报

只生成下面四个一级内容，标题和顺序保持不变：

```markdown
# yyyy-MM-dd 日报

## 一、今日完成工作

## 二、今日未完成工作

## 三、今日遇到的障碍、风险、痛点、堵点

## 四、今日沉淀内容
```

写作规则：

- 第一项只写功能完整且用户确认归属的当天工作；用“完成了什么能力、解决了什么问题、当前呈现什么结果”描述，不按文件清单罗列实现细节，也不写测试/构建通过、命令执行成功、Git 工作区状态或 Skill 自身的名称、目录、脚本、配置、图标、参考文档、内部编号。
- 第二项只写功能不完整的工作、停留位置和对使用结果的具体影响；不把“未提交”本身写成问题，也不暴露实现这些工作的内部包文件。测试、构建或环境失败只有在确实造成未完成时才写其影响，不罗列验证命令。
- 第三、第四项只写用户最终保留或修改后的候选，并用不了解项目的人能读懂的语言说明影响或复用价值。其他用户的记录不写入本人完成项。
- Git 作者、提交者、分支、短哈希、工作区和上游状态只用于内部核对，默认不进入正文。只有用户明确要求精确追溯，或版本状态直接造成无法使用、评审或发布时，才说明必要线索和实际影响。
- 每个工作点至少回答“做成了什么、为什么重要、是否闭环、当前有什么可观察结果”；证据留在内部，不把验证手段写成成果，不编造业务价值、用户反馈、完成度或 push 事件。
- 没有内容时用一句事实说明，不用泛泛的积极结论；不要添加“总结”“下一步计划”等第五项，下一步只能放在相关未完成点或风险中。

## 8. 做 Humanizer-zh 风格检查

定稿前读取 `references/humanizer-zh.md`，对整篇草稿做一次去模板化处理。保留四个固定标题、用户选定的事实、数字和可观察结果；只改表达，不改变证据含义。测试、构建、命令执行和 Git 状态继续作为内部证据，不得因 Humanizer 改写而重新进入正文。项目文件名只有在第二个读者确实需要核对时才保留，Skill 自身的实现文件一律不进入日报正文。

重点删除空泛意义、宣传口吻、模糊归因、填充连接词、虚假范围、三段式堆砌、过度破折号、粗体、表情符号和“这不仅……而是……”式排比。用具体动词和名词、简单句式、长短交错的正常工作语气替代它们，不为制造“人味”添加用户没有说过的感受。

完成条件：逐段检查过直接性、节奏、具体性、事实完整性和精炼度；四个部分的每条内容都能追溯到当前用户相关记录、确认归属的工作区 diff 或用户选择，并且没有把内部实现清单误当成工作成果。

## 9. 保存并验证

1. 创建 `docs/daily-report/`（若不存在），以 UTF-8 写入当前仓库根目录下的 `docs/daily-report/${REPORT_DATE} 日报.md`。
2. 写入后检查文件存在、标题日期正确、四个一级标题存在且顺序正确；确认文件不含 TODO、占位符、空条目、敏感凭据、未解释的推断、原始 Git 字段堆砌，且默认正文没有测试/构建通过、命令执行成功、未提交、未推送、工作区状态或上游状态等研发过程描述。
3. 对日报正文做一次内部名称扫描：检查是否出现 generate-daily-report、Skill 自身名称、skill-<id> 目录、SKILL.md、README.md、LICENSE、agents/openai.yaml、assets/icon.svg、references/humanizer-zh.md、scripts/collect_git_evidence.sh 等包内实现信息；发现后改写为能力和结果描述。项目自身具有明确业务含义的同名文件不受此条影响，但必须说明用途。
4. 保存成功后返回文件链接和一句简短说明；不要自动 commit、push 或修改其他文件。

完成条件：目标文件可读取，路径和命名完全匹配，四个部分齐全，所有工作结论都能追溯到匹配 Git 记录、对应 diff 或用户回答。

## 参考来源

- grilling：<https://github.com/mattpocock/skills/blob/main/skills/productivity/grilling/SKILL.md>
- Humanizer-zh：<https://github.com/op7418/Humanizer-zh>（去模板化规则参考）
- 本 Skill 包内的 `README.md` 和 `LICENSE` 是针对日报 Skill 重新撰写的说明与许可文件。
